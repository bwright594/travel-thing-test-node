class Picture {
  constructor(id, name, person, url, mediaType, date) {
    (this.id = id),
    (this.name = name),
    (this.person = person),
    (this.url = url),
    (this.mediaType = mediaType),
    (this.date = date);
  }
}

export default Picture;
